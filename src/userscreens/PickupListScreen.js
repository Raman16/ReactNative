import React from 'react'
import { Text,View } from 'react-native';
import { SafeAreaView } from "react-navigation";

const PickupListScreen = () => {
  
  return (
    <SafeAreaView>
      <Text>PickupListScreen</Text>
    </SafeAreaView>
  );

};
export default PickupListScreen